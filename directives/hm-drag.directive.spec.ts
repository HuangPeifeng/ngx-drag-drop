import { HmDragDirective } from './hm-drag.directive';

describe('HmDragDirective', () => {
  it('should create an instance', () => {
    const directive = new HmDragDirective();
    expect(directive).toBeTruthy();
  });
});
