import { HmDropListDirective } from './hm-drop-list.directive';

describe('HmDropContainerDirective', () => {
  it('should create an instance', () => {
    const directive = new HmDropListDirective(null);
    expect(directive).toBeTruthy();
  });
});
