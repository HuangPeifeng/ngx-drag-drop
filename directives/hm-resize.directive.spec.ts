import { HmResizeDirective } from './hm-resize.directive';

describe('HmResizeDirective', () => {
  it('should create an instance', () => {
    const directive = new HmResizeDirective();
    expect(directive).toBeTruthy();
  });
});
